# 流切换同步优化

## 问题

- [x] `clean` 命令会删除缓存文件（Saved/, Intermediate/, DerivedDataCache/），导致每次切换后需重新编译着色器
- [x] `clean` 需要扫描所有文件，10万+文件时很慢

## 方案

使用 `.p4ignore` 自动判断哪些文件需要保留，实现智能清理。

### 文件分类逻辑

```
对于本地每个文件：
├── 在 have list 中？ → 保留（depot 管理）
├── 匹配 .p4ignore？ → 保留（缓存/忽略文件）
└── 否则 → 删除（多余文件）
```

### 同步流程

- [x] p4 opened 检查未提交修改 → 有则拒绝切换
- [x] 切换 Stream 和 Root
- [x] sync -k 更新 have list
- [x] 读取并解析 .p4ignore 规则
- [x] p4 have 获取 have list（depot 文件 → 本地路径）
- [x] 遍历工作区 → 删除（不在 have list 且不匹配 .p4ignore）的文件
- [x] diff -se → sync -f --parallel 覆盖内容不同的文件
- [x] diff -sd → sync -f --parallel 下载缺失的文件

## 修改文件

- [x] Source/Data/P4Core.py
  - [x] 新增 GetOpenedFiles（检查未提交）
  - [x] 新增 GetHaveList（获取 have list 路径集合）
  - [x] 新增 GetMissingFiles（diff -sd）
  - [x] 新增 GetDifferentFiles（diff -se）
  - [x] 新增 SyncFiles（sync -f 指定文件）
  - [x] 新增 P4IgnoreParser 类（解析 .p4ignore）
  - [x] 新增 DeleteObsoleteFiles（遍历删除多余文件）
  - [x] 修改 RunSync 支持 flush_only 参数
  - [x] 删除 RunClean 和 CountCleanFiles
- [x] Source/Logic/Callbacks.py
  - [x] OnApply 添加切换前检查
  - [x] _RunSyncAndClean 改用新方案
- [x] Source/UI/UIComponents.py
  - [x] 移除或调整"自动清理"复选框

## 工作区缓存与增强功能

### 需求

- [x] 记住每个流对应的工作区目录，避免重复输入
- [x] 允许用户手动浏览选择目录
- [x] 切换前检测 P4V 是否运行，避免冲突
- [x] 目录不存在时提示并可自动创建

### 修改文件

- [x] Source/Data/WorkspaceCache.py（新增）
  - [x] GetCacheDir 使用 platformdirs 获取跨平台缓存目录
  - [x] WorkspaceCache 类管理每个客户端的工作区目录缓存
- [x] Source/Data/P4Core.py
  - [x] 新增 IsP4GUIRunning 检测 P4V 进程（跨平台）
- [x] Source/Logic/Callbacks.py
  - [x] OnClientSelected 加载客户端对应的缓存
  - [x] OnApply 添加 P4V 检测和目录创建逻辑
  - [x] OnBrowse 浏览按钮事件
  - [x] _UpdateWorkspaceFromCache 根据缓存更新工作区显示
- [x] Source/UI/UIComponents.py
  - [x] 新增浏览按钮
  - [x] SetWorkspaceState 目录不存在时显示灰色
- [x] requirements.txt
  - [x] 添加 platformdirs 依赖
